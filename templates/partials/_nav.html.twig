<nav id="navbar" class="navbar navbar-expand-lg" data-bs-theme="dark">
	<div class="container-fluid">
		{% if is_granted('ROLE_VISITOR') %}
			<div class="mb-4">
				<div class="">
					Bonjour
					{{ app.user.pseudo }}
				</div>
				<div>
					<a class='btn btn-secondary mx-2' href="{{ path('app_logout') }}">
						Déconnecter
					</a>
				</div>
			</div>
			<a class="navbar-brand" href="{{ path('app_home') }}">Accueil</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			{% set entities = [
				'hero_class',
				'hero',
				'item',
				'race',
				'npc',
				'dialogue',
				'effect',
				'answer',
				'biome',
				'event_type',
				'event',
				'ending',
				'review'
			] %}

			{% if is_granted('ROLE_ADMIN') %}
				{% set entities = entities|merge(['user']) %}
			{% endif %}

			{% for entity in entities %}
				<div class="collapse navbar-collapse" id="navbarColor02">
					<ul class="navbar-nav me-auto">
						<li class="nav-item text-end">
							<a class="nav-link text {% if app.request.get('_route') ends with 'app_' ~ entity ~ '_index' %} active {% endif %} " href="{{ path('app_' ~ entity ~ '_index') }}">{{ entity|capitalize }}</a>

						</li>

					</ul>
				</div>
			{% endfor %}
		</div>
	{% endif %}
</nav>
