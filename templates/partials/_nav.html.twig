<nav class="navbar navbar-expand-lg bg-dark" data-bs-theme="dark">
	<div class="container-fluid">
		{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_GAMEMASTER') %}
			<a class="navbar-brand" href="{{ path('app_home') }}">Acceuil</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			{% set entities = 
                [
                'user',
				'review',
				'hero_class',
				'hero',
				'item',
				'race',
       			'npc',				
				'dialogue',	
				'answer',
				'effect',		
                'biome',
				'event_type',
                'event',               
                'ending'
                ] 
            %}
			{% for entity in entities %}
				<div class="collapse navbar-collapse" id="navbarColor02">
					<ul class="navbar-nav me-auto">
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ entity|capitalize }}</a>
							<div class="dropdown-menu">
								<a class="dropdown-item" href="{{ path('app_' ~ entity ~ '_index') }}">Liste</a>
								<a class="dropdown-item" href="{{ path('app_' ~ entity ~ '_new') }}">Créer</a>
							</div>
						</li>

					</ul>
				</div>
			{% endfor %}
		</div>
		<div class="mb-4 ">
			Bonjour  
			{{ app.user.pseudo }}
			<a class='btn btn-secondary' href="{{ path('app_logout') }}">
				Déconnecté
			</a>
		</div>
	{% endif %}
</nav>