<form method="post" action="{{ path('app_dialogue_delete', {'id': dialogue.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dialogue.id) }}">
    <button class="btn btn-danger float-end m-1">Supprimer</button>
</form>
